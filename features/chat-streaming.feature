@P0 @chat
Feature: 聊天会话流式消息

  作为用户，我发送消息后，
  应该看到 AI 助手的回复以流式方式逐步显示，
  包括文本、推理过程、工具调用结果和内联图片。

  Background:
    Given 用户已打开应用 (http://localhost:1212)
    And 用户已配置好至少一个可用的 AI 模型

  # ============================================
  #  基础文本流式响应
  # ============================================

  @CHAT-STREAM-001
  Scenario: 发送文本消息并接收流式回复
    Given 用户在会话列表中创建了一个新的聊天会话
    When 用户在输入框 (placeholder: "Type your question here...") 中输入 "你好，请做一下自我介绍"
    And 用户点击发送按钮或按下 Enter 键
    Then 应该在消息列表中看到用户消息气泡，显示 "你好，请做一下自我介绍"
    And 助手消息区域应该出现加载指示器 (Loader 转圈动画)
    And 助手消息的文本内容应该逐步增长，而不是一次性出现
    And 流式响应完成后，加载指示器应该消失
    And 助手消息应该包含完整的 Markdown 渲染文本

  @CHAT-STREAM-002
  Scenario: 流式响应过程中可以停止生成
    Given 用户已发送一条消息，助手正在流式生成回复
    When 用户点击停止按钮 (在助手消息区域的加载指示器附近)
    Then 流式生成应该立即停止
    And 已接收到的部分文本应该保留显示
    And 加载指示器应该消失

  # ============================================
  #  推理过程 (Reasoning/Thinking)
  # ============================================

  @CHAT-STREAM-003
  Scenario: 显示模型的推理过程
    Given 用户使用的模型支持推理输出 (如 Claude/DeepSeek 等带 thinking 的模型)
    When 用户发送一条需要复杂推理的消息，如 "9.11和9.8哪个大？仔细想想"
    Then 助手消息中应该先出现推理区域 (ReasoningContentUI 组件，可折叠区块)
    And 推理区域应该显示推理文本内容
    And 推理区域展示推理耗时 (duration)
    And 推理完成后，下方应该接续显示最终的文本回答

  @CHAT-STREAM-004
  Scenario: 推理区域可以折叠和展开
    Given 助手消息中包含推理区域
    When 用户点击推理区域的折叠按钮
    Then 推理文本应该被折叠隐藏
    When 用户再次点击推理区域
    Then 推理文本应该重新展开显示

  # ============================================
  #  工具调用 (Tool Call)
  # ============================================

  @CHAT-STREAM-005
  Scenario: 显示网页搜索工具调用过程
    Given 用户已开启网页浏览功能 (Web Browsing)
    And 用户使用的模型支持工具调用
    When 用户发送 "今天北京天气如何" 这类需要实时信息的消息
    Then 助手消息中应该出现工具调用部件 (ToolCallPartUI)
    And 工具调用部件应该先显示 "call" 状态 (正在调用)
    And 调用完成后应该更新为 "result" 状态，并显示搜索结果
    And 最终文本回答应该基于搜索结果生成

  @CHAT-STREAM-006
  Scenario: 工具调用失败时显示错误状态
    Given 用户已开启网页浏览功能
    And 网络连接异常或工具调用失败
    When 用户发送需要工具调用的消息
    Then 工具调用部件应该显示 "error" 状态
    And 应该包含错误信息描述

  # ============================================
  #  内联图片 (模型返回图片)
  # ============================================

  @CHAT-STREAM-007
  Scenario: 显示模型返回的内联图片
    Given 用户使用的模型支持在流式响应中返回图片 (如 Gemini 图片生成)
    When 用户发送 "画一只猫" 之类的图片请求
    Then 助手消息中应该出现图片元素
    And 图片应该正确加载显示 (不是 broken image 图标)
    And 点击图片应该能够放大查看 (PhotoSwipe gallery)

  # ============================================
  #  消息操作
  # ============================================

  @CHAT-STREAM-008
  Scenario: 重新生成助手回复
    Given 助手已完成一条回复
    When 用户hover助手消息，点击 "Reply Again" 按钮
    Then 应该创建新的分支 (fork)
    And 助手应该重新生成回复
    And 新回复应该以流式方式显示

  @CHAT-STREAM-009
  Scenario: 复制助手回复内容
    Given 助手已完成一条回复
    When 用户hover助手消息，点击 "copy" 按钮
    Then 应该显示 "copied to clipboard" 提示
    And 剪贴板中应该包含助手回复的纯文本内容

  @CHAT-STREAM-010
  Scenario: 删除消息
    Given 会话中有至少一条用户消息和一条助手消息
    When 用户点击消息的更多操作菜单 (三点图标)
    And 选择 "delete" 选项并确认
    Then 该消息应该从会话中移除
    And 消息列表应该立即更新
