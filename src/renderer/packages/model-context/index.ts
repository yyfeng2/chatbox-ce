/**
 * @deprecated Use `@/packages/model-registry` instead.
 * This module is kept as a backward-compatibility re-export wrapper.
 */
export {
  DEFAULT_CONTEXT_WINDOW,
  getModelContextWindow,
  getModelContextWindowSync,
  getModelContextWindowWithDefault,
  prefetchModelContextData,
} from '../model-registry'
